# 编译产物（由 go build 生成，不入库；用户自行本地构建）
csdn-mcp
csdn-mcp.exe
csdn-mcp.exe~
*.exe
*.exe~

# 本地真实配置（可能含 Cookie 等凭证，绝不入库；仅提交 .example）
config.json

# 凭证 / 密钥文件
*.pem
*.key

# 项目内部记忆与本地临时目录（可能含内部备注，不外发）
.workbuddy/
